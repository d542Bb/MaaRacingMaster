# 上界按「下一 major」收口：发布包由 requirements-runtime-lock.txt 精确复现，
# 本文件服务源码安装——没有上界时，源码用户会随上游大版本漂出已验证组合
# （numpy 3.x / OpenCV 6.x 等破坏性变更会直接打穿运行时）。
maafw>=5.11.1,<6
vgamepad>=0.1.0,<1
onnxruntime-directml>=1.24.0,<2
rapidocr>=3.9.2,<4
opencv-python>=5.0.0.93,<6
numpy>=2.4.6,<3
windows-capture>=2.0.1,<3
psutil>=5.9.0,<8
# 注意：
# 1. ultralytics 已移出运行时依赖（仅 tools/training/ 训练用，见 pyproject [project.optional-dependencies].train），
#    避免在发布包中拉入 torch 等训练期依赖，大幅减小体积。
# 2. strenum 是 maafw 的运行时依赖，勿删。
# 3. psutil 供 OCR 的 P-core 亲和性绑定（treasure/ocr.py PIN_P_CORE_AFFINITY）：
#    曾长期缺失导致绑核从未生效（E-core 漂移防护缺位），2026-09-14 补为正式依赖。
