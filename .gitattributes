# =============================================================================
# 行尾与二进制策略
#
# 目标：文本文件在仓库内与工作区统一 LF，任何平台（Windows / Linux / CI）检出
# 的字节完全一致。v4 真源（pipeline/*.json、policy/*.json）的 round-trip 保真与
# 内容哈希比对依赖该不变量：行尾若按平台默认（core.autocrlf）漂移，MPE 保存与
# migrate_v4 产物的 diff 会被行尾噪音淹没。
# 例外：tools/navkit/*.cmd 必须 CRLF+ASCII（cmd 批处理解析器对 LF/非 ASCII 会
# 错位碎裂，实证见 v4-p3-studio plan）。
#
# 生效方式：改本文件只影响后续 checkout/normalize，已存在的工作副本需执行一次
# 归一（见 git add --renormalize 或等价的工作区行尾重写）才会落到位。
# =============================================================================

# 默认：git 自动识别文本；仓库与工作区均为 LF，检出不做 CRLF 转换
* text=auto eol=lf

# Windows 批处理：cmd.exe 对 LF-only 脚本的标签/续行解析不可靠，单独要求 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# 资产真源的来源哈希口径依赖 LF，显式声明以防默认规则被改动时静默失效
*.json text eol=lf

# 二进制：禁止任何行尾转换与文本 diff（模板图与发行物被误转即损坏）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.exe binary
*.dll binary
*.pyd binary
*.lib binary
*.pdb binary
*.onnx binary
*.whl binary
*.zip binary
*.7z binary
*.db binary
*.mp4 binary
