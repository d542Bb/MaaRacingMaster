# speedrush 记分观测与规则实测（C 类实验，2026-09-17）

> **本文是什么**：驾驶页 HUD 的读数链路、**每格 ROI 的复核结论**与**记分规则实测结论**——各加分项折算多少分、分数以什么形态出现、能否从录制数据事后复算。
> **本文不是什么**：不是游戏规则的真源（外部事实归 [`RULES.md`](../../../maaracing_master/plugins/speedrush/RULES.md)，本文只提供实测证据供其回填复核）；不是控制方案（见本机 `docs/plan/speedrush-control-route.md`）。
> **信源等级**：L4（过程与证据）—— 可作「这个事实当时怎么验的、结论与复现命令」的依据；结论经维护者复核回填 `RULES.md` 后才升为 L0。
> **继承**：实验规范见 [`tools/experiments/README.md`](../README.md)（C 类两条硬约束、资产分界、目录约定）。

回答的问题：**「极速狂飙」驾驶阶段的分数是怎么来的**——各加分项折算多少分、分数在画面上
以什么形态出现、能否从录制数据里事后复算。这是「胡萝卜模型」（决策层收益模型）的输入规格：
规则没测清之前设计收益模型，等于拿印象当系数。

***

## 方法与判据

- **素材**：`data/speedrush/demos/` 下的已录会话（一轮两阶段各一段，1280×720）。录制帧含完整
  HUD，故**规则实测不必等新实机**——离线即可读数。
- **读数链路**：`probe_hud_ocr.py`。标定用 `discover`/`scan`（带文本检测，先定位再定 ROI），
  生产口径用 `read`/`timeline`/`formula`（关文本检测，与 treasure OCR 同口径：固定 HUD
  文字框下 det 是冗余计算）。
- **ROI 真源**：`hud_regions.json`。`scan` 的**按位置统计**只负责给候选，**每一格都必须过
  下面的复核**才留下——本轮复核推翻了三个候选（见「复核结论」），说明"扫出来"不等于"压对了"。
- **面板在场闸门**：`field_on_dark()`。左侧信息面板会闪烁、且有"矮版"变体（不显示合计），
  直接对 ROI 做 OCR 会读到天空、产出垃圾值。判据是 ROI 内暗像素占比（纯 numpy，零 OCR 开销）。
- **标尺独立性**：读数来自画面（PIL 读盘 → RapidOCR），与"策略应当怎样"无关；
  结论一律以**同一局内多个字段互相印证**为准，不靠单点。

## 复核判据：本轮踩出来的四个坑

复核 ROI 时，"读到字了"和"没读到字"**都不能单独当证据**——两头都会骗人：

1. **框里没东西时 rec 也会强行解码**。关 det 的 rec 被强制出一行文本，于是护栏的水平高光
   读成 `-`、车尾贴纸读成 `TXT`/`VNI`/`甲`、树叶读成 `D`。判"真读到东西"必须要求
   **中文/字母/数字**（正则 `[\u4e00-\u9fff\dA-Za-z]`），不能只判"非空"。
   实测代价：据此选出过"事件横幅场景"帧，框里其实是护栏。
2. **暗底闸门会被深色景物骗过**。面板闸门（暗像素占比）在棕榈树上是达标的。故面板在场
   判据必须是「闸门 **且** 三个字段都读出数字」。
3. **det 对小型 HUD 文字会失明，但这不是 ROI 错**。左侧面板三格在同一帧上 det 报"0 块"，
   而 rec 三个数全读对——det 视角的"框内无字"与"框压错了"是两回事，别据 `dump` 单独定罪。
4. **偶发元素不能先猜框再验证**。浮字跟着车在屏幕上跑（实测 `+30`/`+120` 横跨
   x 0.38–0.59、y 0.44–0.70），固定 ROI 只能偶尔撞上。正确顺序是**先扩大窗口用 `find`
   找真实文字块**，由命中坐标给出该元素在哪，再决定要不要定框——甚至决定它根本不该定框。
5. **"读数对"不等于"语义对"**。左侧面板三格每一格都读对了（与画面数字逐一相符），但我据
   "看起来单调递增"把它判成"本阶段累计里程"——**读数正确、解释错误**。判语义必须用
   "它该不该单调、该不该重置"这类**跨帧性质**去验，不能只看单帧读没读对。
   本轮据此撤回了"里程是累计里程"与"里程+超车占约一成二"两条结论（见结论 2/3）。
6. **暗底闸门会漏，且没有可靠的二次在场判据**。面板不在场时，暗色景物（建筑、车辆）能满足
   "暗像素占比"而骗过闸门，识别模型再把它强行解码成单个数字——本轮据此产出过一次假读
   （帧 365 "里程=2"，看着像"重置到 0"，目视核对发现面板整个不在场）。
   **试过用"标签在场"做二次判据，不可行**：取 `里程`/`超车` 两个标签位做 ROI，31 个采样帧
   **零命中**（读出来是 `用`、`一田` 这类碎片——标签字太小）。因此离线稀疏采样下，
   "面板在场读出一个小数字"与"面板缺席读出噪声"**无法从单帧区分**。
   消费方（实时读数）应以**时间连续性**为二次判据（段内里程单调），而不是只看单帧——
   这是把这条坑真正修掉的位置。

## 复核结论（逐格，`overlay` 出的图 + `find`/`dump` 的原始输出为据）

| 区域 | 判 | 依据 |
|---|---|---|
| `stage` | ✅ | 读出 `阶段:1`；文字只占框内 y 0.04–0.98 的一部分、右侧留空约两成，框偏大但不影响读数 |
| `timer` | ✅ | 读出 `00:25`/`00:08`，与倒计时结论一致 |
| `mileage` / `overtake` / `total_left` | ✅ | 面板在场帧上同帧读出 `338` / `1` / `368`，与画面数字逐一相符。**注意这三格的值会重置**（见结论 2）——那是游戏行为，不是 ROI 问题 |
| `score_top` / `score_bottom` |  读数准、**名字曾误导** | 读出 `560` / `593`（另一帧 `2449` / `1495`），与画面大字相符；但两块面板**会上下互换**（见下「另一处复核发现」）。原命名为 `score_self`/`score_opp` 是错的（暗示"上＝我方"），已按位置改名 |
| `event_banner` | ✅ | `find` 命中实测文字块，右边界最大到归一化 0.1625 < 框右 0.178；最长形态 `极限超车×6` 也容得下 |
| `rate_top` / `rate_bottom` |  留用待查 | 读数不稳（同一帧一侧读出 `41`、另一侧空）；且「得分速度」定义未明（与 `d(总分)/dt` 未对上，见缺口）。同样受"上下互换"影响，故一并按位置命名 |
| ~~`hp_self` / `hp_opp`~~ |  已移除 | 框内混进玩家名——`dump` 实测框内 **2 块**：`吸哥鸡肉卷` + `血量:97%`，rec 会连成一串（曾读出 `血量:396%`、`血量:99`）。且 `RULES.md` §4.9 已定「血量由积分差决定」，与双方总分冗余 |
| ~~`center_popup`~~ | ❌ 已移除 | 浮字**跟着车跑**，无固定位置；没浮字时该框压在玩家车尾，读到的是车贴纸/车牌（`TXT`/`VNI`/`甲`）——**依赖玩家所选车辆的外观**，与硬约束冲突 |

**被移除的三格没有丢信息**：血量是积分差的派生显示，分数本身已直接可读；浮字若要读，
应按 `find` 的做法在一条带内搜索，而不是钉一个框。

### 另一处复核发现：两块比分面板**会上下互换**

右上两块比分面板（本机玩家=蓝底、对手=红底）**不是固定上下**。按"带内饱和像素均色"逐帧判定，
一段之内互换多次、每次互换后各稳定一段：

| 帧区间 | 上块 | 下块 |
|---|---|---|
| 25–43 | 本机(蓝) | 对手(红) |
| 49–85 | 对手(红) | 本机(蓝) |
| 91–391 | 本机(蓝) | 对手(红) |
| 397–565 | 对手(红) | 本机(蓝) |

触发条件未知（得分反超？事件？动画？），已记入缺口表。**后果**：任何按"上／下"判定敌我的读数
都不可信——这正是原 `score_self`/`score_opp` 命名埋的坑（本轮据此产出过一条无效结论）。
判定敌我必须按**块的颜色**：探针的 `block_side()` 取该带内**饱和像素**均色，而不是固定采样点
——固定点会被天空骗过（实测：天空同样满足 B>R，于是判出"下方永远是我方"这种假结论）。

`overlay` 出的图里每格比分都会标出当前归属，可直接看图核。

## 结论

### 1. 合计分值 = 里程 + 30 × 超车 `[实测]`

同帧三个字段同时可读时，逐点比对（`formula` 模式，跨会话）：

| 超车 | 合计 − 里程 |
|---|---|
| 0 | **0** |
| 1 | **30** |
| 3 | **90** |
| 4 | **120** |
| 5 | **150** |

五个档位、两个阶段独立吻合；并与 `RULES.md` §7#4 已记录的观测「里程 338 + 超车 1 → 合计 368」
一致。**另有独立旁证**：画面上的 `+30` 浮字反复出现（见结论 6），与系数 30 同源。

⚠ **少数采样点上公式不成立，但那是假读而非例外**：`合计` 那一格在面板的**矮版变体**下不存在
（该处仍是暗底，闸门拦不住），识别会强行解码出 `92`、`1` 这类小数字，于是出现「合计 < 里程」。
`formula` 模式现在会把这些点单独列出来（实测 84 个采样点里 4 例，且**全部出现在阶段 2**——
提示两个阶段的该面板布局可能不同，未查）。**由公式自身导出的过滤器**：合计 = 里程 + 30×超车 ≥ 里程，
所以「合计 < 里程」必是假读——消费方按这条过滤，不要信单帧的合计。

### 2. 左侧「里程/超车」是**本段**累计，段边界处重置 `[实测]`

**这是本轮撤回的一条错结论**——原先记为"里程是累计里程（1 分/单位），同段内单调递增"。

**证据以逐帧目视核对为准**（不是单帧自动读数）：里程在**同一段内单调增长**，跨段后从较小值
重新增长——

| 帧序号 | 顶部进度条 | 里程 | 超车 | 合计 |
|---|---|---|---|---|
| 209 | 近满 | 338 | 1 | 368 |
| 329 | 约三成 | 272 | 1 | （矮版，无此行） |
| 353 | 约三成 | 278 | 1 | 308 |
| 473 | 约七成 | 259 | 3 | （矮版） |

`338 → 272` 不是"掉头"，而是**换了一段**：两者之间面板长时间不在场（帧 233/257/281/377/401/425/449
均无面板），段的边界就落在那段区间里。同段内实测 268 → 338 单调增长（帧 153–209）。
**合计 = 里程 + 30×超车 在每次读数上都成立**，故合计也是"本段"的量，不是本阶段累计得分。
面板顶部那条进度条疑似就是"本段进度"（帧 209 近满对应里程 338，帧 329 约三成对应 272）。

**为什么这条不能用单帧自动读数下结论**：面板不在场时它能被暗色景物（建筑、车辆）骗过闸门，
而识别模型会强行解码出单个数字。本轮就吃过一次——帧 365 自动读出"里程=2"看着像"重置到 0"，
目视核对发现那一帧面板整个不在场（详见下方复核判据坑 6）。**"段内单调、段间重置"这个结论
是用目视核对立的，不是用那条自动读数立的。**

对照右侧：**右上每方自己的分数在阶段内是累计递增的**（同段实测 6 → 3693）。两个"分"不是
同一个量，**不可互相换算**。段的边界是什么（检查点？门架？时间片？）未测出，见缺口表。

顺带确认了面板形态：**它会闪烁，且有"矮版"变体**（只显示里程/超车、不显示合计）——所以读面板
必须逐字段判在场；矮版出现时 `total_left` 那格落在面板下方的天空上。

### 3. ~~里程 + 超车 只占总分的一小部分~~ —— **该条撤回，占比无法由左侧面板得出** `[已撤回]`

原先的算法是「同帧 左侧合计 ÷ 右上总分」（得出一成二之类）。但结论 2 已证左侧合计是**会重置的
计数**，拿它当"本阶段累计的里程+超车得分"去和累计总分相除，**分母与分子不是同期的量**，
比例没有意义。

仍然成立的是：**右上总分在阶段内涨到数千**（实测到 3693），而左侧滚动计数在几百量级——这说明
累计总分里**绝大部分不来自左侧那两项**，与 `RULES.md` §4.2 的「主要加分项＝金币」`[权威]`
方向一致。但**"占多少百分比"这句话本实验给不出**，需先测清左侧计数的重置规律，或改用
分数增量的来源归因（见缺口表）。

### 4. 阶段计时是**剩余时间倒计时**，且两阶段不对称 `[实测]`

`timer` 单段有效 73/76、**单调性零违例**（35→1）。跨会话：阶段 1 上限约 35 秒，阶段 2 约 4 分 50 秒
——与 §7#5 记录的「阶段 1 计时 00:28、阶段 2 计时 04:54」吻合。

### 5. 事件横幅是**持续显示的连击计数**，不是"事件一闪而过" `[实测]`

这是本轮**修正了的**一条。横幅文字 `极限超车×N` 会连续存在数百帧，且计数递增；它不是某次超车的
瞬时提示。实测序列（一轮之内，跨两阶段）：

- 阶段 1：先出现 `极限变道`，随后转为 `极限超车`，再累加到 `极限超车×2`；
- 阶段 2：`极限超车`（即 ×1）→ ×2 → ×3 → ×4 → ×5 → ×6。

推论：**连击档位是画面上可直接读出的字段**，不必靠分数增量反推——`RULES.md` §7#1（连击判定）
由此从"无素材"变为"有直读量"。注意横幅**只在阶段内首次「极限」事件之后才出现**（此前该位置
是护栏/远景），故判在场必须要求中文/数字，否则会把护栏认成横幅（见「复核判据」坑 1）。

### 6. 浮字是**贴着车跑**的，位置不固定 `[实测]`

`find` 在整条路面上搜到两类浮字：`+30`（反复出现，与 30/超车 系数同源）与 `+120`（以及更大的
`+180`/`+1880` 形态）。它们的归一化位置横跨 x 0.38–0.59、y 0.44–0.70——**没有固定位置**，
因为它浮在被超车辆/自车所在处。故：**浮字不能当固定 ROI 读**；`+120` 的语义（是否等价于
4×30、是否计入「我方总分」）仍待确认，**不能当"超车=120分"用**。

### 7. 画面自带「N米」是**对手车的距离标注**，不是可用观测量 `[维护者口径]`

这是本轮**纠正了的**一条错结论。原先记为"游戏自身给出到目标的距离，世界模型可直接受益"——
**错**。维护者口径：`N米` 标的是**对手车**的距离；对手是**幽灵车**，速度完全由双方得分决定
（分高则显示更靠前，反之更靠后），不参与物理场景、不会干涉本车。故：

- 它对驾驶决策**无用**——没有一个动作能改变它与本车的相对关系；
- 它是**分数差的另一个显示形态**，与右上双方总分冗余，不提供新信息。

**教训**：路面上的文字不都是"可用的世界观测量"，先问"这个量由什么决定、有没有动作能影响它"。

## 复核路线（按条目，给维护者逐条核对用）

> **这是派生索引，不是真源**：规则本身归 `RULES.md`（带 `·待复核` 后缀的条目就是待核对的那批），
> 本节只回答"看哪张图 / 跑哪条命令 / 怎么快速证伪"。核对通过后去掉 `RULES.md` 里的 `·待复核` 即可。

| RULES 条目 | 证据在哪 | 怎么快速证伪 |
|---|---|---|
| §4.7 计时器是倒计时、两阶段不对称 | `timeline` 的计时列 + 单调性检验块 | 跑 `timeline`，看计时列递减、违例为 0；阶段 2 段首看它从约 5 分钟往下走 |
| §4.8 **合计 = 里程 + 30 × 超车** | `formula` 的档位表与残差 | 跑 `formula`，看超车 0/1/3/4/5 时「合计−里程」是否为 0/30/90/120/150 |
| §4.8 面板会闪烁、有"矮版"变体 | `overlay` 的 panel 图；`total` 列大量为空 | 看 panel 图（矮版没有合计那一行）；或在 `timeline` 里数 `total` 的 None 比例 |
| §4.8 里程**段内单调、段间重置** | 目视核对：帧 209 里程 338（进度条近满）vs 帧 329 里程 272（约三成） | 打开这两帧看左侧面板与顶部进度条——**别用自动读数**（面板缺席时会出假读，见坑 6） |
| §4.8 右上两块面板**会上下互换** | 逐帧颜色判定的分段（25–43 / 49–85 / 91–391 / 397–565） | 跑 `overlay`，看两格比分的 `[本机(蓝)]`/`[对手(红)]` 标注是否与画面块色一致 |
| §4.8 左侧合计与右上分数**不可互相换算** | 结论 2、3；`formula` 的「合计 < 里程」假读清单 | 同帧比对左侧合计与右上分数，看两者是否同量级变化 |
| §4.8 浮字**随车移动、不固定** | `find` 的命中坐标（横向跨 x 0.38–0.59） | 跑 `find --window 0.12,0.20,0.92,0.95 --match '\+\s?\d+'`，看命中位置是否分散 |
| §4.8 事件横幅是**持续显示的连击计数** | `find` 的横幅序列（阶段 2 内 `极限超车` → ×6） | 跑 `find --window 0,0.30,0.45,0.70 --match '极限\|连击'`，看是否连续存在且计数递增 |
| §4.9 `N米` 是对手车距离、对决策无用 | 维护者口径（2026-09-17） | 你的确认即可；画面证据只到"它随分数差变化" |

`RULES.md` §7 那一批 `[需实测]` 是**尚未测出**的未知项（连击清零条件、极限超车触发、金币车道分布、
段的边界等），核对方式与素材缺口列在下方缺口表。

## 缺口与下一步

| 项 | 状态 | 需要什么 |
|---|---|---|
| §7#4 金币/连击折算 | 未解 | 金币数不是 HUD 字段；只能从「我方总分 − 合计」的增量与画面金币对齐反推，需要**金币可见的连续帧段** |
| §7#1 连击判定（累积/清零） | **有直读量了** | 横幅给出档位（结论 5），配合分数增量可查时间窗与中断条件（撞车是否清零） |
| §7#2 极限超车触发 | 部分 | 已有 `极限超车`/`极限变道` 两种横幅与 `+120` 帧；需与"贴身距离"观测量对齐——注意**不能用 `N米`**（结论 7），得另找量 |
| §7#3 金币的车道分布规律 | 未解 | 需金币的横向位置序列（属感知层产出） |
| 比分面板上下互换的触发条件 | 未解 | 逐帧颜色判定已能给出互换时刻；需把互换时刻与"得分反超 / 极限事件 / 段边界"对齐，才能判它是不是有意义的信号 |
| 左侧「里程/超车」的**段边界**判据 | 未解（结论 2） | 已知段内单调、段间重置；**段的边界是什么**（检查点 / 门架 / 时间片）未知。需连续帧段 + **逐帧目视核对**（面板闪烁，且缺席时会产生假读，见坑 6）把边界标出来。这直接决定「本段得分」能不能当决策输入 |
| 面板在场判据的漏（坑 6） | 已知、离线不可修 | 标签判据 31 个样本零命中；留给实时读数用"段内里程单调"做时间一致性判据 |
| 分数增量的来源归因 | 未解（结论 3 撤回后） | 要替代被撤回的"占比"说法，应改为给**总分增量的来源**归因：把每个增量区间与画面浮字（`+30`/`+120`）及金币数对齐 |
| `rate_top`/`rate_bottom` 的定义 | 未对上 | 与 `d(总分)/dt` 未吻合，且同帧读数不稳；先不下结论。若长期对不上，考虑它并非"分/秒"而是别的量 |
| `+120` 的语义 | 未确认 | 是否等于 4×30、是否计入「我方总分」；需 `+120` 帧前后与分数增量对齐 |

**另有一项架构决策**（不属本实验）：实时读数（模块内观察线程写 `hud.jsonl`）需要插件内的
OCR 引擎，而 `speedrush` **不能** import `treasure.ocr`（插件自包含契约：整个目录拷走即卸载）。
故把引擎段抽到 core（共用者今天已真实存在两个）。本探针按实验自包含约定**不** import 它，
故探针内保留一份最小引擎——那份副本只服务离线复核，不构成生产口径的第二真源，
但**它的调参需要跟着 core 走**（改 core 时同步核对）。

## 复跑

```bash
P=tools/experiments/speedrush_scoring/probe_hud_ocr.py
python $P scan       # 区域候选（带 det，按位置统计）
python $P dump       --rects hud_regions.json --frame <jpg>       # 框内到底有几块字
python $P find       --window 0,0.30,0.45,0.70 --match '极限|连击'  # 偶发元素定位（先搜后定框）
python $P overlay    --rects hud_regions.json                     # 真帧叠框 + 框内实际读到的字
python $P timeline   --session <会话目录> --rects hud_regions.json
python $P formula    --rects hud_regions.json
```

本探针属 `tools/experiments/`，豁免 ruff 门禁（负结果也是证据，价值在可复跑）。