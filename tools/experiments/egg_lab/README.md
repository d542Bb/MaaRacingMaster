# egg_lab —— 员工守则彩蛋调试试验区（重建 v5）

status: active

## 目的

生产实现（6a048c2）用户报告有 bug、且躲避手感需多轮调校。本区承载**当前生产代码的
逐行同构副本**（常量 / `wireEggDodge` / `showRulesDialog` / `openModal`+`onClose` /
掉落逻辑），迭代确认后修回生产并退役。

## 迭代史（均在 lab 内先行）

- **[lab-fix]** 躲避越界（右对齐行不对称余量，原实现按钮可飞出弹层右缘）、按钮宽度
  钉住防 taunt 换字跳位。
- **[lab-iter2]** 用户反馈"被边框束缚逃不起来"：躲避场 = 整张弹层卡，二维逃跑。
- **[lab-iter3]** 撞墙滑移（被钳掉的分量沿自由轴重分配，角落困不死）+ reach 140 +
  守则列表滚动条隐藏（滚动保留）。
- **[lab-iter4]** 堵逃课出口（`openModal` 新增 `closeOnOverlay`，守则弹层点空白不关，
  Esc 保留）；抓到 = 亮下一句（`EGG_CATCH_LINES` 六句，末句亮起后放弃躲避，再抓一次
  才关门）；躲避本身不再换字。
- **[lab-iter5]** 第 9 条常态化（原第 0 条修订声明变普通规则编号 9，固定显示）；第 10 条
  幽灵行——数字「10.」乱码解码落定（reactbits.dev DecryptedText 手法自实现，40ms×20 帧
  自左向右锁定；许可 MIT+Commons Clause，未复制代码），规则正文默认隐形、悬停显影、
  移出复隐（JS 挂 enter/leave）。

## 当前结构

守则弹层 = 池 12 条每次抽 8（编号 1-8）+ 第 9 条固定（修订声明）+ 第 10 条幽灵行。
抓捕序列六句推进，末句后放行；点空白不关；关闭绿点回响；掉落每第 5 次混怪谈短语。

## 自检

「跑自检」断言：编号结构（10 行、第 9 条固定）、第 10 条悬停显影/复隐、乱码落定、
接近触发二维逃离、出界扫掠（36 位置视觉矩形收在卡内 inset）、跑距上限（≤ reach）、
抓捕序列、点空白不关、通用弹层点空白仍可关（回归）、onClose 单次、末句后再抓关门、
绿点回响、掉落混入频率（25 点 5 中）。

## 退出计划

手感与文案确认 → 修复 pathspec 回生产 → 结论迁 home（app.js 注释 + 前端 README）→
仪器留档（git 历史）→ 删目录。
