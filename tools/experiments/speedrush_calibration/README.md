# speedrush 地面标定：像素→车道坐标的反求实验（方案已对齐，Gate −1 已过）

status: active

> **本文是什么**：「固定追车相机下，把检测框底边反求成 (车道坐标, 纵向距离)」的**采纳方案、
> 对外部调查的三处更正、验收闸门**。它是 [`speedrush_trick`](../speedrush_trick/README.md)
> 撤回结论的续作——判"极限超车是否由贴近触发"（`RULES.md` §7#2）所需的尺子，从这里出。
> **本文不是什么**：不是完整实施记录（Gate −1 已过、结果见「实施顺序」；Gate 0~4 未跑，
> 逐闸补进）。外部调查报告原文**不进树**（调查过程材料，
> 契约见 AGENTS 红线 6）：采纳与更正全在本文，原文需要时从 git 历史
> （commit [`78778a3`](https://github.com/d542Bb/MaaRacingMaster/commit/78778a3f18943a3d3591c2ada55df29f084f6855)）
> 取回或向维护者要工作台副本；下文引用"报告 §N"均指该原文。
> **信源等级**：L1 侧的方案裁定（采纳/更正清单是本项目的决定）+ L4 依据。
> **继承**：实验规范见 [`tools/experiments/README.md`](../README.md)（C 类两条硬约束）。

## 为什么现在做这个

trick 实验的撤回证明：现有像素量（`|cx−锚|`）测不了"贴近"——透视收敛把它污染成假信号
（三证见 trick README ②）。缺的不是深度模型，是**一条像素→地面的标定映射**。
标定到手后按序解锁：① §7#2"贴车触发"用**车道单位**重测（含"极限组偏右 18/22"线索的真伪）；
② 阶段 B 感知规格定案（框 → (车道号, 横向偏移, 纵向距离, 相对速度)）；③ 收益模型的状态空间。

## 采纳的方法主干（外部调查 §1~§20，与本项目失败分析互证）

- **标定源只用静态几何**：车道线/边线/护栏的候选线段（Canny + HoughLinesP）→
  **VP 是统计量不是两线交点**（成簇投票 + RANSAC）→ 得 (VPx, y_h)。
  动态量（目标轨迹、HUD 速度）**只进验收、不进拟合**——这正式定性了 planning 实验
  `y_h=315` 的失败：运动学与投影参数耦合，车的相对速度伪装成透视参数。
- **参数模型**：`(y_h, H, f)` 三自由度（cx 固定 640——本项目已实测 82% 帧 |自车偏移|≤40px，
  报告"待实测"清单里这一条可划掉）；工程上等价于 `(y_h, A_x, A_z)`。
  映射 `X = A_x·(u−cx)/(y_h−v)`、`Z = A_z/(y_h−v)`，**接地点用框底边 v**（本项目口径已如此）。
- **平直段选数据**：标定只吃平直路段帧；弯道/坡道/跳台用 `flat_confidence` 旁路
  （VP 时间稳定性 + IPM 后车道宽随距漂移），**不做坡度估计**。
- **排除清单**（与 vision README 独立同结论）：单目深度网络、SLAM、学习式车道回归、
  运动学拟合当标定源——全部排除，理由见外部报告 §36。

## 三处更正（报告的现实道路先验在本作不成立）

1. **车道宽不是已知米数**。报告的主尺度观测（"车道宽 3.5 m"）是真实道路先验；本游戏的
   车道宽**未知**，且"游戏渲染参数"不可得（不碰引擎内部）。→ **尺度两级化**：
   **横向以"车道宽"为单位**（判"贴近"够用——判据本来就是"半个车道"量级）；
   **纵向用 HUD 米速钉 f·H**（里程 1 分/米 `[三方来源·待复核]` → 实测自车 ≈42 m/s）。
   报告的"第三选择（HUD 位移）"对我们是**唯一选择**，"第一选择（游戏镜头参数）"不可用。
2. **Gate 3 的静止目标假设不成立**。街车在动，超车定义即 ego 更快。→ 纵向米制校验改用
   **车道虚线以自车速度回流**（静态纹理的 dZ/dt 应 ≈ HUD 自车速度）；目标轨迹的 dZ/dt
   只查量级自洽（相对接近速度 ∈ (0, v_ego]）。
3. **y_h 先验区间（已被 Gate 0 实测取代）**。原裁定：路面带实测起于 y≈180、最远检出
   y≈213 贴地平线（vision 实验）→ `y_h ∈ [180, 260]` 作拟合边界。
   **Gate 0 实测推翻该带**：四场 demo 无带交点云（MAD 15~30）与逐帧 VP 一致给出
   y_h ≈ 323~335，原推断链「最远检出车底 = 最远可见路面」不成立（检出器最远车底
   受探测范围限制，且 213 处可有无车远路）。**现行先验（维护者裁定 2026-09-19）**：
   在当前固定追车相机、当前测试条件下，多场数据独立留出验证显示 y_h 稳定集中在
   **约 323px**；该值作为**当前标定先验，而非普适常数**。实现上：标定器的 y_band
   只承担防发散的宽诊断带 [150,420]，323 先验由 Gate 0 的 S4 冻结基准承载
   （`%TEMP%/sr_calib/yh_base.json`，来源场次可溯）。连带疑点：实测 323 与 planning
   实验的「失败解 315」接近，该失败归因（运动学耦合）需重审——不影响 trick 撤回的
   三证（透视收敛假象），只影响 planning 侧叙述。

另记：里程"1 分/米"本身是 `[三方来源·待复核]`——所有米制结论（含 Gate 3）带此不确定性，
车道单位结论不受影响。

## 验收闸门（先写死后开工，任一红即停、按 §38 归因，不加码换模型）

| 闸门 | 判据 | 红时的归因方向 |
|---|---|---|
| **−1 合成自检** | 用已知 (cx, y_h, A_x, A_z) 生成投影线，标定器反推须回到 ±ε。**VPx/y_h 的 ε 实施时定为 z-score（误差 ÷ 估计器自报 σ ≤3）而非绝对 px**——原「±2px」拍在 σ≈2px 的噪声底上，判据错置（合成下 σ 已知准，只有 z 才验数学自洽；绝对精度归 Gate 4 跨场 CV）。A_x ±5%/8%、A_z ±8%、留出 d/σ_d ≤1.5 不变 | 数学实现错，不碰真帧 |
| **0 VP 稳定** | 平直段逐帧 VP：VPx 638±?、y_h 带内且帧间稳；**叠图目视**（线簇真的射向该点）——**✅ 放行**（实施记录含通过含义与三项审计） | 线段族不纯（护栏/对向/建筑吸走） |
| **1 不变道不变性** | 未变道目标的 X_t 漂移中位 < 0.25 车道宽——**✅ 相对车道几何通过；绝对横向位置维持未决**（维护者裁定 2026-09-19，两仪器分开答见实施记录；不得表述为「绝对横向已验证」） | y_h 错 / pitch 漂 / 接地点错 |
| **2 车道号阶跃** | X 直方图多峰、峰距 ≈ 1 车道宽——**🟡 待定（维护者裁定）**：物理刻度+消自车偏移后标定侧 α≈0.99（19 轨），留场 5/8 过、失败全在 n=4 轨的小样本场；瓶颈已转为直道轨样本量，需补录素材，见实施记录 | 横向尺度错 |
| **3 纵向一致** | 虚线回流速度 ≈ HUD 自车速度（±15%）；目标接近速度量级自洽 | f·H 错 / 平面假设失效 |
| **4 跨会话窄带** | ≥3 场独立标定，参数 CV < 5% | 标定器不稳，**不得用下游精度掩盖** |
| **留出** | 70% 线证据拟合、30% 只验收 | 拟合自洽 ≠ 解释世界 |

### 闸门实施记录

- **−1 合成自检：绿**（`2163370`）。20 训练 + 24 未见种子全绿，报告
  `%TEMP%/sr_calib/gate_minus1.json`。VPx/y_h 判据定为 z-score（≤3），A_x 双路径
  max 误差 ≤1.3%、A_z ≤4.2%、留出 d/σ_d ≤0.8。
- **0 VP 稳定：✅ 放行（维护者裁定 2026-09-19，进入 Gate 1）**。
  **通过含义**：筛选结果不存在明显的时序选择偏差，且保留帧具有稳定几何证据
  ——**不是**「86~92% 的帧客观上不适合标定」（该命题证据不足，不作主张）。
  - **分级筛帧**（`probe_gate0_vp.py`）：**S1 证据充分性与几何可用性**
    （候选线 ≥8、长线 |Δv|≥150 ≥2、近端截距簇 ≥3、强内点 σ_d≤12px ≥3、
    强内点中位标准化残差 ≤4——原独立 S2「共点性」四场实测淘汰率恒为 0，
    已按裁定并入 S1 作子条件）→ **S3 比赛态**（score OCR 覆盖处生效，辅助，
    覆盖不到如实跳过）→ **S4 一致性检查**（y_h 距冻结基准 ≤25 + 滑窗
    |dx|≤12、|dy|≤8）。阈值先验固定，依据见脚本注释。
  - **S4 不变量（裁定要求）**：S4 的 y_h 基准**永远来自被筛数据集之外**——
    `--freeze-base` 从指定场次集合建立冻结基准（`%TEMP%/sr_calib/yh_base.json`
    含来源场次），日常运行只读；留出验证用其他场次/其他时段。任何「本场数据
    自定本场基准」的路径不存在（无基准时 S4 整级跳过，不自动回退）。
  - **S4 定位（裁定措辞）**：S4 是**鲁棒异常值剔除阶段，会改变最终估计**
    （收缩分布、移动中位 2~3px）——它不是「只剔尾巴不影响估计」；其正当性
    由留出验证背书：基准来自其他场次时同样收窄，排除用当前样本结果偷偷
    定义当前样本真值的数据泄漏。
  - **审计证据**：① 时间轴（`%TEMP%/sr_calib/timeline_all.png`，四场逐帧）：
    拒绝不成段独占场景、A 帧遍布全程，最长连续 R 段 3.4~11.7s（220453 的
    11.7s 在场尾与结算衔接）；原因分布 S1 24~43%、S3 0%、S4 11~12%。
    ② 被拒帧随机抽检（`rejected_<session>.png`，带淘汰原因标注）：多数有
    明确独立干扰（遮挡/阴影/车流），部分为边缘保守剔除；剔除机制与 VP 真值
    无关。③ 留出验证：跨场留一 + 场内时间留出，B 段 y_h 322.6~324.9
    ±0.2~2.5、VPx 635~653 ±2~11，与 A 段一致（220453 后 30% 无帧通过，n=0
    如实记录）。
  - **y_h 结论**（见更正 3 现行先验）：比赛态 y_h 322.7~323.2 ±1.6~2.5
    （四场），冻结基准 325。比赛态帧占比 8~14%——表述为「筛选器判 86~92%
    帧不适合标定」。
  - 叠图：`%TEMP%/sr_calib/gate0_overlay/<session>/sheet.png` 四场。
- **1 不变道不变性：相对车道间距验证通过；绝对横向验证未决（维护者裁定
  2026-09-19）**（`probe_gate1_invariance.py`）。两问两仪器，不得互推：
  - **相对几何 = 框内夹车虚线间距 s(d)：通过**。漆面线天然不变道；陡线段端点
    采样、按 d 三分位箱独立聚类配「夹 0」两侧簇对，y_h 错 ε 使
    s(d)=s_true·d/(d−ε) 随 d 变化。三场 23 有效帧，漂移中位 **0.154 车道**
    （逐场 0.140 / 0.040 / 0.203，p75 0.220，max 0.520）< 0.25。
    **口径限制（维护者裁定）：整体横向平移在两线间距中完全相消——s(d) 平坦
    只证相对车道几何/尺度一致，不证绝对横向零点无偏移。**
  - **绝对横向 = 车辆轨真值分析：未决**。前提由维护者确认：AI 目标车不变道、
    不转向 ⇒ 横向真值恒定，X_t 的一切系统性漂移都是标定/场景信号。样本只用
    n/时长/距离筛（禁按 X_t 筛），三场 10 轨（n≥24）。四项报告：
    ① **X_est 随距离漂移**：raw 漂移中位 0.128 车道；逐轨拟合
    X_obs = a + b/d + c·d 的 **b（X~1/d 斜率）9/10 为负、中位 −41 道·px**——
    已证明的是：**存在与深度相关的系统性项**（VPx/零点假说需 δ≈−70px，与
    Gate 0 VPx 641~653±11 量级矛盾，排除）；道路曲率（横向偏移 z²/2R，1/d 的
    二次项）能解释其量级与形状、是当前最自洽候选，但**不是已证归因**——
    #65 远端 X=−3.95 道（最左道仅 −2，物理不可能）证明的是远端受深度相关的
    几何污染，来源可为曲率/接地点/y_h 之组合。含 1/d² 的 4 参拟合退化
    （1/d 与 1/d² 在窄 d 范围共线）→ **深度相关项与零点在单轨内不可分**。
    ② **绝对 lane bias**：a 对整数车道回归零点偏 +0.19 道、尺度 α=0.914、
    max|bias| 0.46 道——深度相关污染下是**上界**不是估计。
    ③ **轨迹内波动**：模型残差 rms 中位 0.15 道。
    ④ **归因**：y_h——b 对 a 回归斜率 ε=−5.6px 但跨轨 fit rms 20（不可辨识）；
    VPx/零点——被深度相关系统项吞没；A_x/尺度——α=0.914（上界）；接地点
    Δz/A_z≈0.006（c∝a 斜率，A_z 定标归 Gate 3）。
  - 自车平移的共模扣除试过两条路都不成立：逐帧 VP 被估计噪声主导（用它重算
    X 漂移反升至 0.35~0.56 道，不可作位姿真值）；夹车虚线中心 c_f 是正确信号
    但噪声（IQR ±0.25 道，虚线碎块不对称）大于真实自车扫移，消共模后漂移
    中位反升至 0.222 道。banner 只覆盖计分的极限变道，覆盖不了全部自车横移。
  - **解锁条件（并入 Gate 2 准备）**：绝对检验需直道段门控——图像空间曲率
    检测（车道线二次项，免标定、不读 X）筛帧 + 非 trick 场次（自车横移少）；
    达成前不得宣布「绝对横向位置验证通过」。
  - **维护者裁定（2026-09-19）**：Gate 1 通过，准确名称 =「**相对车道几何
    通过**」；绝对横向位置维持未决；README 与交付物不得出现「绝对横向标定
    已验证」类表述。
- **Gate 1b 直道对照复验 + Gate 2 首跑（2026-09-19，`probe_gate2_hist.py`）**。
  - **免标定直道门控**已立：判据 = 两侧线族（近端截距聚类，最靠近画面中心的
    左右各一）端点对最优直线的矢高 ≤40px。阈值由十场扫描定标（直道场中位
    24~30px vs 弯道场 73~237px，40px 落两峰谷底；原拍 5px 在噪声底以下，
    全 0% 判直，诊断叠图实锤）。不读 X、不用 VP/y_h/A_x。trick 三场全在
    弯道高架（直道 0%）——非 trick 场 213104_p1/215616_p1 比赛段直道 ~45%。
  - **曲率假说被对照实验支持**：直道轨（10 条，n≥12、d 比≥1.3，不按 X 筛）
    b(X~1/d) 负号 4/10、中位 +8.7 道·px——对照全弯道样本（9/10 负、−41）
    **系统性负偏坍缩**。深度相关项的主要来源是路曲率，不是标定零点
    （措辞：支持≠已证——直道轨 n 尚小，且 b 的噪声底未独立标定）。
  - **Gate 1b 绝对横向首次可判读数（直道、曲率自由度已拿掉）**：跨场 lane
    回归零点偏 **+0.065 道**、尺度 α=0.901、max|bias| 0.49 道（n=10）。
    零点偏远小于 0.25 判据——绝对横向从「未决」进到「初步通过，待扩样」。
    max|bias| 0.49 的轨落半道位（round 归属可疑），扩样时查。
  - **Gate 2 首跑：红（如实记录，判据未动）**——直道接地点 X 直方图
    （n=150/165）有峰但峰距杂乱（0.11~1.27，非 ≈1.0）。当时归因 = 横向
    尺度错（lane 回归 α=0.901，两个不同测量口径得到一致结论——工程交叉
    验证，非统计独立，维护者措辞修正）。
- **Gate 2 交叉验证与归因翻转（2026-09-19，维护者设计：标定/留场交换，
  只允许 A_x 动，vpx/y_h/零点冻结）**：
  - 第一轮（A/B 标定→C 留场）过、第二轮（C/D 标定→A/B 留场）不过——
    诊断出 **α 估计本身被两处管线错污染**：① 刻度源混用晶格 A_x（四场
    0.413/0.473/1.350/2.049，跨场差 5 倍；物理口径 0.563/0.560/0.580/
    0.574，CV≈1.8%）；② 自车换道未计分则 banner 不剔除，自车处半道位时
    轨 a 整体 +0.5 道（213104 实证 4 轨中 3 轨 frac 0.35~0.69，α 被拉至
    0.754——非尺度错，是 lane 回归前提破坏）。
  - 修复 = 刻度源物理优先 + 轨 a 减 c_f 窗中位消自车偏移。**结果归因翻转：
    标定侧 α=1.024/1.002（≈1），物理口径 A_x 无需重定标**——首跑红是
    测量管线错，不是 A_x 真偏小 10%。
  - 留场验证（判据先写死：α_val∈[0.95,1.05] 且 |zero_bias|<0.25）：
    C 0.979/−0.003 过；B 1.015/−0.190 过；**A 0.888/+0.092 不过（仅 4 轨，
    自车机动质量差）**——如实报告不隐藏。max|bias|≈0.49 的半道位轨在各场
    反复出现（真实半道位行驶或 c_f 消偏残差），扩样时查。
  - **扩样轮（2026-09-19，纪律执行：A_x/VPx/y_h/判据全冻结，只加素材
    215701_p2/200530_p2）**：标定侧 19 轨（C/E/F）α = **0.986**、零点
    +0.011——全局尺度干净。留场 8 次判定 5 过 3 不过，失败模式有规律：
    **不过的三场全是 n=4 轨**（A 0.888→0.902 两次、215701 0.770），
    n≥6 的场全部 α_val∈[0.98,1.02]。215701 的 0.770 与 A 的 ~0.90 不稳定
    复现，**不符合「场次特定系统误差」应有的稳定模式**，更符合小样本
    lane 回归 + c_f 消偏残差（窗中位噪声 ~±0.08 道/轨）的合成。
  - **瓶颈转移**：Gate 2 的矛盾已从「尺度对不对」转为「直道轨样本量」——
    直道门控严（全场 2~6%）∧ d 比≥1.3 ∧ n≥12 后，多场只剩 4 轨。demos
    十场已扫尽（191511_p1 全弯道不可用），**需补录直道比赛素材**才能
    把定级做到可靠水平。在此之前 Gate 2 维持 🟡（维护者裁定：不带着
    未解释的 0.888 进 Gate 3）。
- **补录素材扩样轮（2026-09-19，维护者补录 6 场、可用 3 场：163352_p2/
  163557_p2 直道 75%、163152_p2 50%）**：
  - 标定侧（新 3 场 25 轨）α=1.023、零点 +0.054；反向（旧 3 场 21 轨）
    α=1.012——**全局尺度持续干净**。留场：B 0.994 ✓、C 0.979 ✓、
    163352 1.039 ✓、163557 1.040 ✓、**A 0.870 ✗（第三次稳定复现
    0.87~0.90）**、163152 0.950 ✗（临界，且该场物理 A_x=0.710 为唯一
    离群值，刻度源在该场疑被污染）。
  - A 场按裁定进入「场次特定系统误差」怀疑程序。诊断三步：
    ① **跨线假说提出即证伪**——A 轨 frac 稳定 0.47/0.58 疑为自车半道位
    （c_f 对 ±0.5 道有周期歧义），加免标定跨线检测器（近端截距落画面
    中心 ±80px 的线族）后 **0 命中**：自车没跨线，c_f 不该失真。
    ② 逐轨叠图（`g2_A_tracks.png`）给出实锤替代解释：**#42 第三帧框跳到
    右侧红车（tracker 换车污染，其 rms 0.23 四轨最大）**；**#50 目标车
    全程稳定在右车道偏左位置——AI 不变道 ≠ AI 在车道中心**。
    ③ 结论：A 场 α_val≈0.87 的机制是轨质量（换车）+ 道内偏移，不是
    标定尺度。「固定车道 AI 轨 ⇒ a ∈ 整数道」前提需弱化：横向真值恒定
    但**不必然是道中心**，lane 回归的 round(a) 归属对带道内偏移的轨不可靠。
  - 跨线检测器保留为门控护栏（v2 缓存；本批素材 0 命中只说明没跨线，
    判据本身正确）。
  - 待裁定（不自行推进）：轨强化（跳变剔除：邻帧 X 残差/框 IoU 断轨）
    或改「轨间差 = 整数道」建模（道内偏移 δ_i 自由，用差分消绝对位）。
- **a+b 落地（维护者裁定 2026-09-19：a 轨迹卫生 → b 有界道内偏移模型，
  A_x 彻底冻结不再被 AI 轨反调）**，`--validate` 模式：
  - 模型 X_i = bias + α·k_i + δ_i 实现中修掉两个**可辨识性 bug**：
    ① k 无界 → 远车配假高阶 k 补偿 δ（首跑 k=11/12 而审计斜率仍 ≈0），
    加物理界 |k|≤3；② δ 界取 0.5 = 格距一半 → 相邻 k 格互简并 + 交替
    拟合小样本崩解（bias 飞 +1.8、δ=±2.6），收紧为车不压线物理界
    |δ|≤0.35 并改 bias 全局网格搜索（Σδ² 最小）。
  - **结果一（AI 仪器）**：卫生层 0 断轨（跳变阈未命中——#42 型换车发生在
    轨末段、不违反邻帧速度界），但 8 场中 7 场 AI 不过：δ max 达 1.7~4.1
    道、bias 卡网格边界 ±0.48~0.6——**部分轨的 a 离格 2 道以上，是远距
    外推爆炸/换车型假轨，不是尺度信号**。AI 交叉验证当前输入质量不足以
    支撑格假设检验；这是仪器质量结论，不是标定结论。
  - **结果二（新主矛盾，几何仪器）**：框内夹车虚线间距 s_med 八场
    1.027/1.061/1.12/1.168/1.117/1.058/1.093/1.138——**系统性 >1、
    中位 ≈1.09、跨场方向全部一致**（判据 [0.9,1.1] 有 4 场出界）。
    s(d) 随 d 平坦（Gate 1a 已证）排除 y_h 大误差解释 → 物理口径
    ax_physical（单行截距间距）与 s_med（逐 d 中点 X 差）两个同源不同式
    的间距测量相差 9%——**尺度的真定义问题浮出，下一轮的主靶**。
  - 纪律执行：A_x/VPx/y_h/判据全程未动；s_med>1 不通过「把 A_x 乘 1.09」
    处理——那正是维护者警告的真值假设污染，须先裁决哪个间距口径对。
- **仪器审计（维护者裁定 2026-09-19：三实验 A/B/C，不动标定参数，
  `--audit` 模式）**——9% 分歧已定位，证据链闭合：
  - **实验 A（固定行扫描）**：Δx(v)/Δx(650) = [0.694,0.771,0.847,0.924,
    1,1.076,1.153]，八场几乎逐位一致——严格线性且斜率精确等于
    (v−323)/327，即**两拟合线交点恒在 (VPx±3px, y_h±4px)**（33 帧实测）。
    针孔外推层干净；「交点 u 偏移」候选排除。审计判读修正：Δx(v) 本应
    线性漂移（过 VP 的直线对的定义性质），A 真正回答的是线性性=已证。
  - **实验 B（同深度 du_obs vs du_fit）**：中位 1.009（d<250）/0.978
    （d<400），±2% 内一致——检测/聚类层无 9%。
  - **d 下限梯度（12/60/120）**：s_med 不响应（1.04→1.16 微升）——
    「远距噪声尾巴」假说证伪。
  - **破案定量**：拟合线口径 Δx_fit@650 = 666~668px（与 Gate 1 直接实测
    夹车虚线 312/980=668px 吻合），而 ax_physical 截距口径 = 581px，
    **偏小 13%**。预言 s_med = 668/581 = 1.15 ≈ 实测 1.09~1.14 ✓。
    guilty party = **ax_physical 的配对逻辑**：取 vpx 两侧「最近簇」量到的
    是虚线内缘/次近结构而非道中心距；同一方法跨场稳定地偏小（偏差方向
    一致）——这恰好解释 s_med 跨场方向一致 >1 的谜。
  - **结论与待裁定**：9% 不在针孔层、不在检测层、不在 AI 轨——在
    ax_physical 的口径定义（仪器 bug 而非标定参数错）。正确口径候选 =
    拟合线法（交点已证在 VP，Δx_fit(650)/(650−y_h) 直接给 A_x）。
    修口径必然改 A_x 数值（~0.563→~0.49 方向），**超出「仪器审计」授权，
    待维护者裁定后执行**；执行后 Gate 1a/1b/Gate 2 全部用新口径重跑。
- **口径修复尝试：四版全被证伪（2026-09-19，维护者批准修 source 后执行）**：
  - v2「两侧最近族（拟合线）」→ 815px：虚线被车身遮挡/截距碎裂时单边缺失，
    配对退化到边线。v3「gap 120 防碎裂 + 对称检验」→ 477/579/725 乱跳：
    放宽段数门槛引入护栏伪对（护栏与虚线恰好对称）。v4「审计 A 族定义
    （gap 60、n≥4）+ 对称 |u_l+u_r−1280|<150」→ 7/8 场 n=0：对称检验
    把仅剩的真对也挡了（真夹车对常单边缺失，凑不齐对称对）。
  - **被证伪的假设类**：「逐帧选两条线就能可靠测车道宽」——夹车虚线在
    比赛帧大多单边可见（遮挡+虚线间隔碎裂），任何逐帧配对规则要么选错
    对象（边线/护栏）要么无数据。唯一可靠证据仍是审计 A 的**正确配对帧**
    （668/666px，对称自洽，与 Gate 1 直接实测吻合）——但它只能当抽检
    锚，不能当自动口径。
  - 代码回滚 v1（全部已裁定结论的冻结口径；v2~v4 的教训入本记录）。
  - **待裁定的新口径提案（仪器设计，非参数调整）**：全局格阵 RANSAC——
    跨帧汇集所有族的拟合线 u@650 位置，车道线族在 {±0.5, ±1.5, ±2.5}·L+φ
    格上（RULES §4），护栏/伪对自然离格；RANSAC 采两点定 (L, φ) 数内点，
    取最多内点格阵的 L。逐帧缺失由跨帧汇集补偿，配对歧义由格阵结构消解。
    与 Gate −1 estimate_ax 的区别：输入 = 跨帧拟合线位置（稳健），不是
    单帧端点 s（被参考系与碎段污染，真帧已证不稳）。
  - Gate 2 状态（维护者裁定 2026-09-19）：**🟡 待定，不算绿**——管线修复后
    参数已接近 1，但留场验证 3/4，通过性待扩样定级。半道位（max|bias|≈0.49）
    是真实候选解释、非结论（c_f 自身有残差，需更多固定车道/自车位置样本）。
  - **扩样纪律（维护者裁定，冻结清单）**：A_x = 当前物理口径值、VPx、y_h、
    投影模型、判据 α_val∈[0.95,1.05]∧|zero_bias|<0.25 全部冻结，不得为让
    A 场过线而调整；下一轮只允许增加 ① 更多直道素材 ② 更多固定车道 AI 轨
    ③ 更可靠的 c_f 消偏。判读：A 场扩样后 α 自然回 ≈1 → 上轮失败=样本/机动
    质量；仍稳定 ~0.89 → 才允许怀疑场次特定系统误差。Gate 3 开工前必须先
    定掉 Gate 2 这颗黄灯，不带着未解释的 0.888 往后冲。
  - 连带发现（移交 Gate 2/4，不在本闸门修）：① 框内夹车虚线间距中位
    1.09~1.20 投影刻度单位 ≠ 1.0 → 当时推断「A_x 刻度系统性偏小 10~20%」
    ——**该假说已被证伪并撤销（维护者裁定 2026-09-19）**：s_med≠1.0 与
    α=0.90 同源，均为晶格刻度混用 + 自车横向偏移未消的管线污染，物理口径
    下 α≈1.0（见下条交叉验证）；② 晶格拟合 A_x 跨场不稳（0.44/0.49/1.33），
    物理近端截距间距稳定（0.56/0.61/0.58，CV≈4%）——晶格锚在真帧被边线/
    护栏污染，Gate 2 起刻度源以物理口径为准（此条仍成立）。
- **全局格阵 L 估计（维护者裁定 2026-09-19：批准「全局格阵约束」假设类，
  实现冻结为 L 全局 + φ_f 逐帧 nuisance + 半整格不预设对称 + 同帧对
  m∈{1..4} 枚举 + 内点按帧去重 + 668 只作事后审计锚；`--globalL` 模式）**：
  - 实现（冻结规则全部落码内注释）：输入 = audit 同款筛帧，帧内取**全部**
    合格线族（≥4 段、矢高≤40、v 支持覆盖 650±10px）的拟合线 u@650——只取
    左右最近族会把逐帧配对假设从输入端带回；残差阈 τ=15px（审计 B 散布底，
    先于运行写死）；得分 = n_obs≥2 帧的逐帧最优相位内点率均值（按帧去重，
    单线帧不计分）；谐波破简并 = 在 argmax 候选的整数倍栈上取得分落差
    ≤0.05 的最大倍数（过走/欠走余量两侧 ≥0.10）；细化 = 同帧双内点对
    |Δu|/m̂ 中位。**A_x source 优先级改为格阵 > 物理 > 晶格**（同一源公式
    A_x=(V_REF−y_h)/L，修 source 不修 output）。
  - **结果：L = 582.1/582.4/583.8/585.2（四场，CV 0.2%）**，格一致性
    0.74~0.87，谐波审计 @1.5L/@2L 得分塌至 0.42~0.50（无谐波解）；5 场
    计分帧 <10 不出 L（阈值 10 先于运行写死，不为覆盖率回调），回退物理。
  - **重大勘误（推翻上轮「破案定量」，证据 `_tmp` 转储已删、数字入档）**：
    7 场全部良构帧逐帧转储，拟合线夹车间距 @650 = 576~591（中位 583）——
    **F 口径 666~668 不复现**；668 只出现在虚线单边缺失、lf/rf 误选非相邻
    线的异常帧（653~679，恰落在当年「正确配对帧」数值域）+ 一次人工直接
    实测（312/980，与全部系统观测的格结构不符，疑为其他地图或误读）。
    **格阵 582~585、拟合线 583、ax_physical 581 三口径一致：「P 口径稳定
    低估 13%」本身是上轮审计的伪像**。措辞（维护者裁定 2026-09-19）：结论
    是 **L≈583 在现有观测体系下高度重复**（三口径汇合），**不是「已证明
    绝对无偏」**——无独立物理真值前不主张无偏；668 降级为误配/误读异常值，
    不再作为后续实验的目标答案——「只有不同几何路径
    才能暴露系统偏差」的方法论在此反证了旧叙事。s_med>1（1.03~1.17）仍在，
    但已成为 **1:3 的孤立仪器矛盾**（dash 端点仪器 vs 格阵/拟合线/截距三
    仪器），且该仪器不经直道门控（弯道帧在内）——留待下轮单独复查，本轮
    不动、不调参。
  - **全链重跑（纪律③）**：Gate 1（trick 三场：无直道帧不出 L，回退物理
    口径——结果与上轮一致：s(d) 漂移 0.154 通过、绝对横向消共模 0.222
    通过、lane 回归 α=0.914）；Gate 2 `--validate` 八场：**逐场复现上轮**
    （s_med 1.027~1.168，4/8 几何过；AI 7/8 不过 = 已定的「输入质量不足」
    仪器结论）——**Gate 2 维持 🟡，全链数值无回归**。
- **dash 端点仪器 s_med>1 破案（2026-09-19，维护者定调：问题=「解释仪器
  为什么偏大」，不是「证明 583 正确」；诊断脚本用完即清、数字入档）**：
  - 诊断链：① 直道/弯道分层——偏差两者皆有、随 d 缩小单调变糟（直道
    1.165@d250-400 → 1.705@d12-80），排除「纯弯道污染」；② 仅贴合格线族的
    端点配对**更糟**（1.41~1.59）——排除「游离段污染」假说；③ 单帧深挖：
    合格线族本身可能是护栏/路缘结构（X@650=−0.73/+0.85，偏离半整格 0.2+
    道），簇 X 范围宽达 0.7 道（真地面线簇 <0.1）；④ **机制证实 = 紧致簇
    配对**：只允许 X 范围 ≤0.15 道的簇参与配对，读数坍缩到 **1.012~1.040**
    （直道与弯道、d150-400 一致），而紧致对可用率仅 28~45%。
  - **机制定论（叠图实锤）**：dv≥60 的 Hough 段大量来自**车辆精灵边缘与
    护栏**（诊断叠图：卡车/自车车身/护栏全被描红、端点密集落在其上）——
    3D 竖直结构的 X=A_x(u−vpx)/(v−y_h) 沿深度涂抹成跨 0.7 道的假簇，恰好
    夹 0。而夹车对按游戏几何恒 =1.0：**任何一端被更远结构替代只会把读数
    拉向 >1.0（结构性单向偏置），不存在 <1.0 的机制**；[0.6,1.4] 带 +
    「最接近 1.0」选择恰好保留 1.0~1.4 的假对。真夹车对只在 ~1/3 帧-箱
    存在 → 删失混合的中位系统性 >1（1.03~1.17）。**s_med>1 从来不是尺度
    信号，是删失混合伪像**；583 口径由此获得第四条独立路径支持（紧致簇
    1.01~1.04）。
  - 待裁定（不自行实施）：① Gate 1a 仪器修复 = 紧致簇过滤（X 范围 ≤0.15）
    + d 下限抬至 150（小 d 处 X 灵敏度 ∝1/d 且相邻线族在 u 上不可分，
    紧致后仍 1.58~1.73 垃圾）+ 报告紧致对可用率；Gate 1a 的**漂移判据是
    相对量、结论不受此案影响**，但其噪声底被同一混合抬高。② Gate 2 几何
    判据 s_med∈[0.9,1.1] 测在删失混合上——修复后重定（预期全过，走裁定
    流程）。
- **裁定执行：dash 仪器修复 + Gate 1a/2 重跑（2026-09-19，维护者五条裁定：
  ①紧致簇≤0.15+d≥150 批准、②可用率入正式输出、③Gate 1a 完整重跑不继承
  旧数、④Gate 2 判据 [0.9,1.1] 不动、⑤重跑出正式结果前维持 🟡；参数冻结
  禁二次搜索）**：
  - 实现：`TIGHT_XRANGE=0.15`、`DASH_D_MIN` 12→150、可用率 =Σlen(sbins)/
    Σn_att 进 gate1 单场/跨场与 gate2 validate 输出（三件套：s_med + 可用率
    + s(d) 分带中位）。
  - **Gate 1a 重跑 = 样本枯竭**：trick 三场（全弯道地图）修复仪器下仅 1 帧
    有效（原 23 帧；该帧漂移 0.007、可用率 2/3 箱）——紧致判据杀掉弯道上
    被曲率涂抹的真簇 + d≥150 移除近场密集观测。判定：**修复仪器在弯道素材
    上不可判相对几何（素材性质，非仪器问题）；旧 PASS 0.154 是污染仪器的
    数字，按裁定不继承**；重验需直道素材。绝对横向仪器不受影响（消共模
    0.216 通过、lane 回归零点 +0.205）。
  - **Gate 2 重跑（判据未动）**：s_med = 1.002 / 1.001 / 0.994 / None /
    1.028 / 0.959 / 1.019 / 1.274（八场序同上；可用率 67~100%，215701 为
    0 箱）——**6/8 几何过（修复前 4/8），通过证据按裁定表述为「修复仪器后
    重新获得」，非「按预期恢复」**。两场不过：215701 无有效观测（直道帧仅
    8）；163152 s_med=1.274——该场物理 A_x=0.710 为已知离群（0.710/0.5615
    ≈1.264 恰解释读数），且其 d 分带 1.277/0.956 分裂，非纯尺度误差。
    AI 交叉验证 7/8 不过（既定「输入质量不足」仪器结论，数值逐位复现）。
    **Gate 2 维持 🟡**。
  - **缓存灾变与重建（2026-09-19）**：360 清除 %TEMP%——sr_calib/sr_trick
    全丢，demo 原始帧完好。按「忠实恢复、不多不少」重建（analyze13/score4/
    banner3/dets12/yh_base/straight11，29 min）：freeze-base 逐位复现
    （325.0/n=841）、直道掩码逐场一致、globalL 四场 L 逐位复现（582.1~
    585.2）。环境事实：派生缓存无 Git 保护且位于 %TEMP%，清理类工具可无声
    清除；是否移位/加白待裁定（未实施）。
- **游戏事实更正（维护者 2026-09-19，外部咨询补样建议的输入前提修正）**：
  比赛**无选图机制**；**道路类型与车辆类型从头到尾一致**（车道结构/宽度/
  标线不变），但**路径生成与沿途风景为肉鸽式、每场不同**。影响：① 特化
  素材的独立性来自**不同的局**（逐场不同的路径/风景天然构成独立条件），
  不来自换图——Gate 1a 补样规则改为「≥3 个相互独立的局，每局长直道充足，
  录制条件与现有素材一致（全速、车道内稳住少变道、不紧跟前车遮挡虚线）」；
  ② 163152 的假设空间收窄：排除「地图固有尺度」（道路类型恒定），保留
  路径/风景逐场变体、检测异常、几何模型下一层缺口三类候选，复核方式 =
  同路另取独立直道片段。
- **特化素材轮（2026-09-19 晚，按裁定补样规则执行；6 新场
  202053_p1~202739_p2，848~1542 帧）**：
  - 素材概况：直道帧占比 1.2~4.4%（与旧素材同量级）；202655_p1 比赛态仅
    10 帧，全程不可用。globalL 扩到 9 场：新 5 场 L=581.9~583.7，跨场中位
    **582.2、CV 0.2%**——「道路类型恒定」从口述事实变成直接测量事实
    （L 不随局变）。
  - **Gate 1a（修复仪器 × 直道素材）：首次有效判定——通过**。5 有效帧、
    紧致对可用率 80%（12/15 箱）、s(d) 漂移中位 0.012 / max 0.015（判据
    <0.25）；有读数场次 s 中位 1.005~1.006。对比：同仪器在弯道素材仅 1 帧
    ——「Gate 1a 的适用域是直道」由裁定变成实验事实。
  - **Gate 1b 绝对横向扩样**：新素材 68 轨（旧素材 10 轨），跨场消共模
    漂移 0.108、lane 回归**零点偏 −0.007 道、α=0.985（n=68）**——绝对
    横向从「初步通过待扩样」进到强支持域。
  - **Gate 2 十四场（判据 [0.9,1.1] 未动）**：**有数据的 9 场全部几何过**
    （s_med 0.959~1.028，可用率 67~100%）；3 场无数据退出（215701/202053/
    202446：直道帧仅 8~11、紧致观测 0 箱）；163152 维持 1.274 结构性离群
    （按裁定保留原样，待同路独立片段复核）。AI 交叉验证 12/13 不过（新
    素材轨更多但 δ 爆炸更烈——「输入质量不足」结论再次成立；可用的 AI
    证据 = gate1 lane 回归 α=0.985）。**Gate 2 维持 🟡，定级待裁定**。
  - 存量 bug 修复：`track_metrics` 的 `elig` 经 numpy 比较链返回 `np.bool_`
    不可 JSON 序列化（新素材轨多样后首次触发）——`bool()` 包装修复，
    无行为变化。

## 实施顺序

1. ✅ `probe_ground_calib.py`：Gate −1 合成自检——标定器本体（VP RANSAC+IRLS /
   A_x 端点逆方差聚类+自车锚定晶格 / A_z 虚线回流网格 LS）已立，
   判据对 20 训练种子 + 24 未见种子全绿（换段复验防种子过拟合）。
   实现期三处关键定论（详见脚本 docstring）：① A_x 取样必须用**端点位置**
   s=(u−VPx)/(v−y_h) 而非斜率——同批数据精度高 20 倍，且斜率口径下夹车虚线锚对不可分辨；
   ② 尺度简并（g vs g/2 晶格同解释整格数据）在纯几何内不可分，破它的外部信息是
   **自车居中 ⇒ 夹车道宽=1 车道**（RULES §4 权威 + 实测锚），据此锚定半整格晶格；
   ③ 合成场景结构须按 RULES §4（自车在第 3 道、虚线在 ±0.5 道），首轮误把分隔线画在
   车正中央，给简并留了温床。
2. ✅ `probe_gate0_vp.py`：Gate 0 真帧逐帧 VP——分级筛帧（S1 证据/几何 →
   S3 比赛态 → S4 一致性）+ 三项审计（时间轴/rejected 抽检/留出验证），
   **维护者裁定放行**，见「闸门实施记录」。
3. ✅ `probe_gate1_invariance.py`：Gate 1——**相对车道几何通过（维护者裁定）**；
   绝对横向（车辆轨真值分析，AI 车横向恒定·维护者确认）维持未决：存在深度
   相关系统项（VPx 假说已排除），曲率/接地点/y_h 单轨不可辨识，解锁条件=
   直道门控+非 trick 场次（并入 Gate 2 准备）。连带定论：真帧 A_x 刻度源 =
   物理近端截距间距（晶格锚不稳），见「闸门实施记录」。
4. ✅ `probe_gate2_hist.py`：免标定直道门控已立（矢高 ≤40px，十场定标）——
   同时完成 Gate 1b 直道对照复验（曲率假说支持；零点偏 +0.065 道，待扩样）
   与 Gate 2 首跑（红：峰距≠1.0，当时归因尺度错 α=0.90——后经查为测量
   管线错，见下一段）。
5. ✅ **Gate 2 🟢 PASS（维护者定级 2026-09-19 夜：定义=几何正确性）**——
   A 项 VALID 10/10 几何过；DASH 可观测率 11/24=45.8% 列为 Operational
   KPI（非否决条件）；163152 结案移出；不降低 DASH_BIN_MIN、不建临时
   产率门槛、不拿 globalL 补位。完整措辞与 manifest 见「当前状态」与
   `DATASET_MANIFEST.md`。
6. **▶ Gate 3 纵向一致**（虚线回流 vs HUD 速度 → A_z）——Gate 2 已闭合，
   按实施顺序进入；Gate 4 跨会话窄带 + 留出，出 `report.json`。
7. 全绿 → 回 trick：车道单位重跑"贴近分离"（含右偏线索）；任一红 → 停，按归因列报告。

## 当前状态（维护者定级 2026-09-19 夜：Gate 2 定义=几何正确性）

> **Gate 1a：🟢 PASS（直道适用域）**
> **Gate 1b：🟢 PASS / 强支持**（68+67 轨、零点 −0.007/−0.004 道、α=0.985/0.976）
> **Gate 2：🟢 PASS——几何正确性。** 正式写法：A 项（几何正确性）在全部
>   VALID 场景中 **10/10 通过**；DASH 仪器观测覆盖率 11/24 作为**独立仪器
>   质量指标（Operational KPI）**记录，不构成 Gate 2 正确性否决条件；
>   13 个 NO_OBSERVATION 不计入几何正确性分母，其完整状态由
>   DATASET_MANIFEST 保留；1 个 OUTLIER_REPLICATED_NOT 已完成独立复核、
>   未复现。措辞纪律：**不写「13 场被排除所以 10/10」**，写「10 个 VALID
>   场景全部通过；其余场景因预定义可观测性原因无法用于该仪器的几何判定，
>   完整状态由 manifest 保留」。
> **DASH observability：11/24 = 45.8%（Operational KPI，非 Gate 判据）**
> **A_x / L：多路径收敛，L≈582px，跨场 CV≈0.2%（13 场）**
> **AI 轨迹：辅助诊断仪器，不作尺度正式依据**
> **163152：OUTLIER_REPLICATED_NOT 已结案，移出 Gate 2**
> **Gate 定义边界（维护者裁定）：Gate 验收「模型对不对」；manifest/KPI
>   记录「测量系统有多好用」。若未来下游需要 DASH 在线测量，另立
>   「运行期可观测率」工程验收指标，与几何正确性 Gate 分离。**
- **补采与 163152 复核轮（2026-09-19 夜，6 新场 210835_p1~211329_p2）**：
  - **Gate 2 整体覆盖重算（裁定要求重算覆盖，非逐场改判）**：判定 19 场
    （202655 比赛态不足跳过）——有数据 11 场、无数据 8 场。**有数据 11 场中
    10 场几何过**（s_med 0.959~1.028，新增 211329=1.007/可用率 67%），唯一
    不过仍是 163152=1.274。旧 3 个无数据场（215701/202053/202446）依旧
    0 箱。
  - **163152 复核判定（三分支，冻结逻辑）→ 分支 2：单次场景/观测异常
    （暂定）**。4 个同路独立片段（不同局=不同路径：202138/202530/202739/
    211329）读数 1.005~1.007 全部恢复 ≈1.0。按判定逻辑不进「模型适用域」
    分支；163152 保留在集合内、其 d 分带分裂（1.277/0.956）仍无解释。剩余
    不可排除项：低频场景变体恰未复现（若存在，归 §7#18 场景变体问题）。
  - **新发现：dash 仪器样本产率塌陷（非仪器错）**。新批 5/6 场直道帧
    8~39 张却 0 箱（210919 达 39 张）。逐帧诊断（临时脚本已清，数字入档）：
    真夹车对**存在**（紧致簇 X≈±0.5、间距 1.0）但**每簇端点数仅 2~4，
    低于 DASH_BIN_MIN=4 的配对门槛**，且单侧常被涂抹簇（+0.5~+0.9 连续
    污染）占用——观测密度问题，不是读数问题（有数据场读数 1.005~1.007
    与既往一致）。同批 globalL 在 13/24 场成功（每帧只需 ≥2 线族）——
    产率差异是仪器结构性的：dash 仪器要求「双紧致簇 × 每侧 ≥4 端点 ×
    ≥2 分箱」，是全场最贵的观测。无数据瓶颈已从「素材不足」转移为
    「dash 仪器产率」。
  - 待裁定：① Gate 2 覆盖口径——是否以「有数据 10/11 过 + 无数据 8 场
    （产率限制、非素材缺失）」闭合，还是要求 dash 产率提升方案（改门槛
    = 调参需裁定，或换用更高产率仪器如 globalL 作覆盖补位）；② 163152
    「暂定单次异常」是否结案（残留：低频变体假设 + d 分带分裂无解释）。
- **数据治理裁定落地（2026-09-19 夜，维护者裁定）**：
  - **样本状态三态制**：VALID（有观测，入分母）/ INVALID（处理阶段错误致
    无效，可剔除但须客观可复核原因，**剔除永不按 Gate 结果决定**；当前为
    空）/ NO_OBSERVATION（数据有效而仪器产率不足，**保留在覆盖率统计、
    不得伪装成 INVALID**——210919 为典型案例：39 张直道帧是真数据，只是
    端点密度未达 DASH_BIN_MIN）；OUTLIER = 保留并记录独立复核状态。
  - **163152 结案**：降级为「独立复核未复现的单次异常」
    （OUTLIER_REPLICATED_NOT），移出 Gate 2 阻塞项；d 分带分裂与低频
    变体假设转**后续观察项**——除非再现第二个同型场景，不再烧预算追孤例。
  - **Gate 2 两项拆分**：A 几何正确性（VALID 10/10 过，已强支持）≠
    B 仪器可观测率（11/24，未闭合）。B 不阻塞 A；若把 B 列为硬门槛属
    **新 Gate 定义**，须单独制定观测产率标准——**不降低 DASH_BIN_MIN、
    不把 globalL 当 dash 补位**（口径不同源，混用会混淆 Gate 2 验证了什么）。
  - **manifest 已立**（`--manifest` 模式 → `DATASET_MANIFEST.md`，24 场
    全量：VALID 10 / NO_OBSERVATION 13 / OUTLIER_REPLICATED_NOT 1 /
    INVALID 0，剔除原因全部数据驱动）。下一步待裁定：本实验是否要求
    dash 覆盖率闭合（=新 Gate 定义 + 产率标准），还是几何正确性即
    Gate 2 的完备目标。
- **Gate 2 定级裁定（2026-09-19 夜，维护者）：🟢 PASS，定义=几何正确性**。
  - 理由（非「数据好看故放宽」）：Gate 2 证明的命题 =「**在仪器获得有效
    观测的适用场景中，标定得到的车道尺度/几何关系与已知 1 车道宽关系
    一致**」——即 A 项；DASH 产率回答的是「仪器产率/数据覆盖能力」，
    是另一个问题。把 B 设为硬门槛 = 把正确性实验偷换成「仪器工程性能
    验收」。证据切分干净：DASH 有数据时几何稳定通过；NO_OBSERVATION 主因
    已定位（直道长度/端点密度）；globalL 在更多场景工作；**无任何证据
    表明「没有 DASH 观测」意味着标定错误**。
  - 措辞纪律：不写「13 场被排除所以 10/10」，写「10 个 VALID 场景全部
    通过；其余场景因预定义可观测性原因无法用于该仪器的几何判定，完整
    状态由 manifest 保留」——覆盖不足不藏掉。
  - **DASH observability 11/24 = 45.8% 升级为 Operational KPI**（不是
    新 Gate）：数字持续走低应推动仪器工程改进，但不污染几何正确性结论；
    若未来下游需要 DASH 运行时在线测量，另立「运行期可观测率」工程验收
    指标。**Gate 验收「模型对不对」；manifest/KPI 记录「测量系统有多
    好用」**——Gate 定义从此不随实验结果漂移。

## 复跑

Gate −1 合成自检（不碰真帧，秒级）：

```
.venv\Scripts\python.exe tools\experiments\speedrush_calibration\probe_ground_calib.py
```

Gate 0 真帧逐帧 VP（约 1 分钟/四场，stride 2）：

```
.venv\Scripts\python.exe tools\experiments\speedrush_calibration\probe_gate0_vp.py --freeze-base <session>...   # 建冻结 y_h 基准（首次/换素材时）
.venv\Scripts\python.exe tools\experiments\speedrush_calibration\probe_gate0_vp.py <session> [<session> ...]     # 筛帧+审计+叠图
```

S4 不变量：基准只来自 `--freeze-base` 的场次集合（写 `yh_base.json`），无冻结基准时
S4 整级跳过；留出验证由每次 report 自动执行（跨场留一 + 时间留出）。

Gate 1（约 1 分钟/三场，复用 Gate 0 缓存 + trick dets）：

```
.venv\Scripts\python.exe tools\experiments\speedrush_calibration\probe_gate1_invariance.py <session>...
```

输出 `gate1.json`（判定与逐场统计）、`gate1_dash_<session>.png`（X–d 散点）、
`gate1_tracks_<session>.json`（车辆轨存档）、`gate1_xseries.png`（X_t 曲线板）。

Gate 1b 直道复验 + Gate 2（非 trick 场需先建 dets 缓存：
`probe_trick_trigger.detections(session)`；直道门控缓存 `straight_<session>.json`）：

```
.venv\Scripts\python.exe tools\experiments\speedrush_calibration\probe_gate2_hist.py <session>...
```

输出 `gate2.json`（矢高门控比例、直道轨拟合、histogram 峰表）。
缓存 `%TEMP%/sr_calib/`：analyze_<session>.json、gate0_overlay/、rejected_*.png、
timeline_all.png；换素材/改参数后删对应缓存重跑。
换未见种子段复验无过拟合：改 `probe_ground_calib.py` 内 `SEEDS` 区间为未训段再跑，判据不应翻红。
外部报告原文的复现价值在其引用链，不在此目录。
