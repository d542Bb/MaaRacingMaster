# ADR-0005：GUI 标题栏——移除系统标题栏，窗口控制由 HTML 自绘

- **状态**：`accepted`
- **日期**：2026-08-31（`ddc1504`）
- **决策者**：用户拍板，AI 落实施
- **相关**：取代 [ADR-0004](0004-GUI宿主定案.md) 的「系统 caption buttons」部分 · [apps/MaaRacingMaster.Shell/README.md](../../apps/MaaRacingMaster.Shell/README.md) · commit `ddc1504`

## 背景

[ADR-0004](0004-GUI宿主定案.md) 定案 WinUI 3 宿主时，标题栏取「HTML 自绘标题栏视觉 + **native caption buttons overlay**」形态（Electron `titleBarOverlay` 类，选型过程见 [归档方案](../plan/archive/titlebar-convergence-plan.md)）：保留系统 —□× 按钮，前端只画标题栏背景与内容。

该形态的运行成本是**系统按钮不认识 HTML 模态层**：系统 caption buttons 画在非客户区，模态遮罩覆盖不到它们，必须由应用自己置灰——为此维护了一整套状态同步（C# `SetCaptionButtonsDimmed` 与 `CaptionBg*` / `DimBg*` 常量、JS `notifyModalState` / `modalOpenCount`、HTML `header-spacer` 占位），且每新增一个弹窗都要接入这套联动（动机与改动清单以 `ddc1504` 为据）。

## 决策

**保留系统边框（可缩放），移除系统标题栏与 —□× 按钮，窗口控制由 HTML 自绘。**

四条具体形态：

1. **非客户区**：`OverlappedPresenter.SetBorderAndTitleBar(true, false)`——保留边框与缩放语义，去掉系统标题栏与按钮。
2. **自绘按钮**：WebView2 内 HTML `win-controls`（SVG 图标）经 `win-action` 消息驱动 C# 执行最小化 / 最大化 / 关闭；`AppWindow.Changed` 上报最大化状态，切换还原 / 最大化图标。
3. **非客户区双区域**：`InputNonClientPointerSource` 注册两类——`Draggable` = 整条标题栏带（`SetDragRectangles`，空白处由系统处理拖动与双击最大化）、`Passthrough` = `.brand` / `.tabs` / `.win-controls` 的精确矩形（输入穿透交给 HTML）。**Passthrough 优先于 drag rects**，故交互区之间的空白仍可拖拽。
4. **置灰联动整体删除**：自绘按钮是 HTML 元素，模态遮罩天然覆盖，不需要任何按钮状态同步。

## 后果

**收益**：
- 模态与按钮的一致性由 DOM 层叠天然保证——新增弹窗不必再接入一套置灰联动；
- 标题栏成为纯 HTML 资产（配色、图标、失焦态与前端同一套 token），消除「HTML 视觉 + 系统绘制」两套渲染的接缝；
- 窗口行为仍由系统承担：拖动、双击最大化、缩放都在 `Draggable` 区，没有退回客户区内自绘拖动。

**代价与约束**：
- **窗口控制按钮、标题栏高度与交互区只能经 `InputNonClientPointerSource` + HTML 维护**，不得改回「系统绘制按钮 + 应用补置灰逻辑」的形态；
- 前端 `.brand` / `.tabs` / `.win-controls` 的矩形必须逐元素上报（`app.js`），否则交互区会被 draggable 带吃掉（表现为双击触发最大化）；
- 尺寸变化须重算两类区域（`AppWindow.Changed` → `UpdateDragRects`）。

**被否方案**：
- **native caption buttons overlay**——系统按钮与 HTML 模态层无法自动协同，需常驻一套置灰状态同步（ADR-0004 定案时的形态，已随本决策删除）；
- **客户区内自绘并自管拖动**（更早的 frameless + JS `begin_window_drag` 路线，见[归档方案](../plan/archive/titlebar-convergence-plan.md)）——失去系统拖动 / 双击最大化 / Snap 与 DWM 动画；本决策不采用该路线，拖动仍由系统在 `Draggable` 区处理。

## 证据

- **实施 commit**：`ddc1504`（2026-08-31）「自绘标题栏窗口控制按钮，移除系统标题栏与置灰联动」——含上述四条形态与置灰联动的删除清单（`SetCaptionButtonsDimmed` / `modalOpenCount` / `header-spacer`）。
- **代码（当前真源）**：[`MainWindow.xaml.cs`](../../apps/MaaRacingMaster.Shell/MainWindow.xaml.cs)——`SetBorderAndTitleBar(true, false)`、`InputNonClientPointerSource.SetRegionRects(NonClientRegionKind.Passthrough, …)`、`minimize` / `maximize` / `close` 消息分支；前端 [`index.html`](../../apps/MaaRacingMaster.Shell/frontend/index.html) `#win-controls` 与 [`app.js`](../../apps/MaaRacingMaster.Shell/frontend/app.js) 的交互区矩形上报。
- **施工事实**：[apps/MaaRacingMaster.Shell/README.md](../../apps/MaaRacingMaster.Shell/README.md)。
- **旧形态（供对照，勿据以实施）**：ADR-0004 正文；归档选型 [titlebar-convergence-plan.md](../plan/archive/titlebar-convergence-plan.md)。